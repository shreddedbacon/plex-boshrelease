#!/bin/bash
set -e

echo "Extracting plexpy..."
plexpy_dir=$(tar tf plexpy/v1.4.25.tar.gz --exclude="*/*")
tar xzvf plexpy/v1.4.25.tar.gz


pushd ${plexpy_dir}
  cp -a . ${BOSH_INSTALL_TARGET}
popd

cp plexpy/daemon_0.6.4-1_amd64.deb ${BOSH_INSTALL_TARGET}
